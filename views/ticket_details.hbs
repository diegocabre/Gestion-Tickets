{{!-- ticket_details.hbs --}}
{{> main title="Detalles del Ticket"}}
<h1>Detalles del Ticket</h1>
<h2>{{ticket.titulo}}</h2>
<p>{{ticket.descripcion}}</p>

<h3>Comentarios</h3>
<ul>
  {{#each comments}}
    <li>{{this.mensaje}}</li>
  {{/each}}
</ul>

<form action="/tickets/{{ticket.id}}/comment" method="post">
  <textarea name="mensaje" placeholder="Añadir comentario"></textarea>
  <button type="submit">Enviar</button>
</form>
